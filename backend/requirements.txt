# AcademIQ backend — auth + student dashboard deploy (Render / Vercel stack)
# Install: pip install -r requirements.txt
#
# For the full ML + quiz stack locally, use: pip install -r requirements-full.txt

fastapi
uvicorn[standard]
pymongo>=3.12,<5
certifi
pydantic[email]>=2
python-jose[cryptography]
bcrypt>=4.0.1,<5
passlib[bcrypt]>=1.7.4
python-dotenv
numpy
